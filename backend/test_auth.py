import urllib.request
import json

url_login = 'http://127.0.0.1:8000/api/auth/token'
# The user usually has an admin setup. Let's try standard credentials or checking the logs.
# Let's just create a mock in the router instead.
